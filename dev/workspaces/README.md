# IDE Workspace Files

This directory collects VS Code workspace configurations used by different teams or environments. Moving them out of the project root keeps the repository tidy while still making them easy to find.

Add new workspace files here and update this README with a short description when needed.
