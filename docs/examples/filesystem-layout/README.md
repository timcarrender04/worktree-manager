# Sample Filesystem Layout

This directory mirrors a representative `WORKTREE_ROOT` structure (`hds-175/Tree/...`) so documentation can refer to a concrete example without polluting the project root.

Feel free to update or replace these samples as new environments are onboarded. If you add more examples, keep them under this folder and document their purpose here.
